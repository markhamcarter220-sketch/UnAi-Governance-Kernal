# Production dependencies (minimal by design)
# The governance kernel has minimal dependencies to ensure
# maximum portability and minimal attack surface

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.990
